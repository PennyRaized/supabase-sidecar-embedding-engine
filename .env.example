# Supabase Sidecar Embedding Engine - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Your Supabase project URL (found in Project Settings > API)
SUPABASE_URL=https://your-project-id.supabase.co

# Service Role Key (found in Project Settings > API > Project API keys)
# WARNING: Keep this secret! This key has full database access
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# =============================================================================
# OPTIONAL CONFIGURATION (with sensible defaults)
# =============================================================================

# Default batch size for processing embedding jobs
# Higher values = faster processing but more memory usage
# Default: 3 (range: 1-5 recommended)
DEFAULT_BATCH_SIZE=3

# Maximum processing time in seconds before stopping
# Prevents Edge Function timeouts on large queues
# Default: 30 seconds
MAX_PROCESSING_TIME_SECONDS=30

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env
# 2. Fill in your Supabase URL and service role key
# 3. Adjust batch size and timeout if needed
# 4. The Edge Function will use these values automatically

# =============================================================================
# SECURITY NOTES
# =============================================================================

# NEVER commit your actual .env file to version control
# Use different keys for development, staging, and production
# The service role key has full database access - keep it secure!
